# num2persian

[![PyPI version](https://badge.fury.io/py/num2persian.svg)](https://pypi.org/project/num2persian/)
[![npm version](https://badge.fury.io/js/persian-number-words.svg)](https://www.npmjs.com/package/persian-number-words)

تبدیل اعداد به کلمات پارسی. در دسترس در پیاده‌سازی‌های پایتون و تایپ‌اسکریپت.

📖 **این را به [انگلیسی](README.md) بخوانید**

## نصب

```bash
pip install num2persian
```

## پیاده‌سازی تایپ‌اسکریپت

این پروژه همچنین شامل پیاده‌سازی تایپ‌اسکریپت به عنوان یک بسته NPM است.

```bash
npm install persian-number-words
```

برای اطلاعات بیشتر، ببینید [typescript/README.md](typescript/README.md).

## استفاده

### API پایتون

```python
import num2persian

# تبدیل اعداد به کلمات پارسی
print(num2persian.to_words(42))        # خروجی: چهل و دو
print(num2persian.to_words(1234))      # خروجی: یک هزار و دویست و سی و چهار
print(num2persian.to_words(-567))      # خروجی: منفی پانصد و شصت و هفت
print(num2persian.to_words("890"))     # خروجی: هشتصد و نود
```

### خط فرمان

```bash
# تبدیل عدد به کلمات پارسی
num2persian 2025

# نمایش نسخه
num2persian --version

# نمایش راهنما
num2persian --help
```

## ویژگی‌ها

- تبدیل اعداد صحیح و اعشاری (مثبت، منفی و صفر) به کلمات پارسی
- پذیرش ورودی‌های رشته‌ای که بتوانند به اعداد تبدیل شوند
- گرامر پارسی صحیح با قرارگیری درست "و" (و)
- پشتیبانی از اعداد بسیار بزرگ با بازگشت به نمایش علمی
- اعداد اعشاری با پسوندهای پارسی مناسب (دهم، صدم، هزارم و غیره)
- رابط خط فرمان
- پوشش تست جامع

## مثال‌ها

```python
from num2persian import to_words

# اعداد پایه
to_words(0)      # "صفر"
to_words(15)     # "پانزده"
to_words(100)    # "یکصد"

# اعداد مرکب
to_words(123)    # "یکصد و بیست و سه"
to_words(2025)   # "دو هزار و بیست و پنج"

# اعداد بزرگ
to_words(1000000)    # "یک میلیون"
to_words(1000000000) # "یک میلیارد"

# اعداد اعشاری
to_words(3.14)   # "سه ممیز چهارده صدم"
to_words(0.5)    # "صفر ممیز پنج دهم"
to_words(1.234)  # "یک ممیز دویست و سی و چهار هزارم"
to_words(12.25)  # "دوازده ممیز بیست و پنج صدم"

# اعداد منفی
to_words(-42)    # "منفی چهل و دو"
```

## توسعه

### راه‌اندازی

```bash
# کلون کردن مخزن
git clone https://github.com/yourusername/num2persian.git
cd num2persian

# ایجاد محیط مجازی
python -m venv venv
source venv/bin/activate  # در ویندوز: venv\Scripts\activate

# نصب در حالت توسعه
pip install -e .
```

### تست

```bash
# اجرای تست‌ها
pytest

# اجرای تست‌ها با پوشش
pytest --cov=persian_numbers
```

### ساخت

```bash
# ساخت wheel و توزیع منبع
python -m build

# نصب وابستگی‌های ساخت
pip install build twine

# آپلود به TestPyPI (اعتبارنامه‌های واقعی را جایگزین کنید)
twine upload --repository testpypi dist/*

# آپلود به PyPI (اعتبارنامه‌های واقعی را جایگزین کنید)
twine upload dist/*
```

## مجوز

مجوز MIT - جزئیات را در فایل [LICENSE](LICENSE) ببینید.

## مشارکت

مشارکت‌ها خوش آمدید! این پروژه شامل پیاده‌سازی‌های پایتون و تایپ‌اسکریپت است:

- **پایتون**: در دایرکتوری ریشه قرار دارد
- **تایپ‌اسکریپت**: در دایرکتوری `typescript/` قرار دارد

هر دو پیاده‌سازی در CI تست می‌شوند. لطفاً آزادانه یک Pull Request ارسال کنید.
